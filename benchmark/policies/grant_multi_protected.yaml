dialect: postgres

# Only users is protected so the multi-object GRANT regression requires detecting
# the second relation (users), not merely the first (orders).
protected_tables:
  - users

rules:
  grant_to_public_on_protected_objects: block
